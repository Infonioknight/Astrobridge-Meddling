so this sort of attention map augmentation will only work in a few cases. So the augmentation itself does not help with adding new signal which, it helps amplify softer signals that a certain model might find useful, and drown out others that are seemingly irrelevant. 

So, ideally we're dealing with the computational overhead of two models.
Now, in my case, using say dinov2 for attention, and then astroptv2 95m for true embedding and result makes some experimental sense since both models are fundamentally different and there could be something that dino brings to the table that astropt lacks (whcih can be proven through seed evaluation to see if there is consistently an improvement in metrics).

now, lets say, I do the same thing, but instead of dinov2, I use astropt 850m as my initial attention model. there wont be any significant useful result since we know for a fact (and experimentally proved in the astropt paper) that 850m >> 95m in terms of performance, so we almost evidently know theyll sort of have a near similar way of doing things, and the bigger one simply does it better -> hence no meaningful signal?
(Now I realise that since astropt is a causal transformer it doesn't have an implicit understanding of say a global representation) - so it does not contain a CLS token, which can give a global representational attention map like dino does.

In more AI-generated understanding based on my claim above:
DINOv2 brings genuinely orthogonal biases to the table — different training objective (self-distillation/invariance losses vs. AstroPT's next-patch regression), different training domain (natural ImageNet-scale images vs. astronomical flux), different architecture class (bidirectional ViT vs. causal transformer). That's exactly the kind of divergence that makes "does model A's notion of salience help model B" a meaningful question — there's a real chance A is picking up on structure B's own pretraining never had reason to emphasize. AstroPT-850M, by contrast, shares the same objective, same data domain, and same architecture family as the 95M model — just more capacity. The paper's own scaling-law finding (smooth, monotonic improvement with size) supports your intuition: within a single architecture/objective/domain, scaling mostly sharpens and refines the same underlying representation rather than discovering a qualitatively different one. So whatever "salience" the 850M model encodes is very likely a better-resolved version of what the 95M model already encodes internally — using it as an attention source would mostly be telling AstroPT-95M to pay more attention to things it was probably already implicitly weighting, just less precisely. Low marginal information.
